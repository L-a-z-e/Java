패키지 : 클래스의 묶음
    - 하나의 소스파일에는 첫번째 문장으로 하나의 패키지 선언만 허용
    - 모든 클래스는 반드시 하나의 패키지에 속해야함
    - . 을 구분자로 계층구조를 구성할수있다.
    - 물리적으로 클래스파일을 포함하는 하나의 디렉토리

자바에서 기본적으로 제공하는 이름없는 패키지(unnamed package) -> 소스파일에서 자신이 속할 패키지를 지정하지 않은 클래스는 모두 한 패키지에 속하는 셈
